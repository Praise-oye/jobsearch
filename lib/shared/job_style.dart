import 'package:flutter/material.dart';

class JobStyle {
  // MISC
  static const borderRadius8 = BorderRadius.all(Radius.circular(8));
  static const borderRadius = BorderRadius.all(Radius.circular(16));
  static const borderRadiusButton = BorderRadius.all(Radius.circular(32));
  static const elevation = 2.0;
  static const elevationH = 4.0;


  //COLORS
  static const white = Color.fromRGBO(255, 255, 255, 1);
  static const black = Color.fromRGBO(0, 0, 0, 1);
  static const purple = Color.fromRGBO(123, 18, 209, 1);
  static const grey = Color.fromRGBO(226, 221, 230, 1);


  
}
